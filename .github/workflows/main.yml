name: Molecular Plus

on:
  push:

  pull_request:

jobs:
  build_windows:
    runs-on: windows-latest
    outputs:
      VERSION: ${{ steps.build.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.13.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.13.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest cython wheel setuptools
      - name: Lint with flake8
        run: |
          flake8 __init__.py creators.py operators.py simulate.py --count --exit-zero --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      - name: Build with Cython
        id: build
        run: "echo skipped-for-baseline-isolation"
      - name: Upload windows zip
        uses: actions/upload-artifact@v4
        with:
          name: molecular-plus_${{ env.VERSION }}_313_win
          path: ./molecular-plus_${{ env.VERSION }}_313_win.zip

  build_linux:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.build.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.13.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.13.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest cython wheel setuptools
      - name: Lint with flake8
        run: "echo skipped-for-baseline-isolation"
      - name: Build with Cython
        id: build
        run: "echo skipped-for-baseline-isolation"
      - name: Upload linux zip
        uses: actions/upload-artifact@v4
        with:
          name: molecular-plus_${{ env.VERSION }}_313_linux
          path: ./molecular-plus_${{ env.VERSION }}_313_linux.zip

  build_macos_arm64:
    runs-on: macos-latest
    outputs:
      VERSION: ${{ steps.build.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.13.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.13.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest cython wheel setuptools
          brew install libomp
      - name: Lint with flake8
        run: "echo skipped-for-baseline-isolation"
      - name: Build with Cython
        id: build
        run: "echo skipped-for-baseline-isolation"
      - name: Upload mac zip
        uses: actions/upload-artifact@v4
        with:
          name: molecular-plus_${{ env.VERSION }}_313_macos_arm64
          path: ./molecular-plus_${{ env.VERSION }}_313_macos_arm64.zip
